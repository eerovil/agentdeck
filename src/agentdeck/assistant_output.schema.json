{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "required": ["status", "summary", "reason"],
  "properties": {
    "status": {"type": "string", "enum": ["blocked", "review", "done"]},
    "summary": {"type": "string"},
    "reason": {"type": "string"}
  }
}
