# Normalize provider sessions and expose dynamic capabilities

AgentDeck translates provider-specific conversations into one Session model and offers operator
actions through dynamic Capabilities rather than provider-name branches in the shared UI. This
keeps presentation provider-neutral while preserving safety: discovery, AgentDeck Ownership,
Runtime Availability, and current controllability remain separate facts that each provider must
project explicitly.
